
using Microsoft.AspNetCore.Hosting;
using System.IO;

namespace top.ebiz.service.Service
{
    public class FileHelper
    {
        //public static string GetFolderPath(string folder)
        //{
        //    // Combine the root path with the folder path
        //    string outputDirectory = Path.Combine(AppDomain.CurrentDomain.BaseDirectory, "wwwroot", folder); 

        //    // ตรวจสอบว่ามีโฟลเดอร์นั้นหรือไม่ ถ้าไม่มีให้สร้าง
        //    if (!Directory.Exists(outputDirectory))
        //    {
        //        Directory.CreateDirectory(pathSave);
        //    }

        //    return pathSave;
        //}
    }
}

